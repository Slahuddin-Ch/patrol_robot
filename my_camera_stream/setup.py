#!/usr/bin/env python

from setuptools import setup

setup(
    name='my_camera_stream',
    version='0.0.0',
    packages=['my_camera_stream'],
    install_requires=[
        'roslibpy',  # Add any dependencies here
    ],
)


