#!/usr/bin/env python

from setuptools import setup

setup(
    name='websocket_ros_bridge',
    version='0.0.0',
    packages=['websocket_ros_bridge'],
    install_requires=[
        'roslibpy',  # Add any dependencies here
    ],
)

